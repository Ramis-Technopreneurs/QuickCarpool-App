import 'package:flutter/material.dart';

class PassengerTab extends StatelessWidget {
  const PassengerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}